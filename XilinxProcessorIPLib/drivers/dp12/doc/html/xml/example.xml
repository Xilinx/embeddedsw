<?xml version='1.0' encoding='UTF-8' standalone='no'?>
<doxygen xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="compound.xsd" version="1.8.5">
  <compounddef id="example" kind="page">
    <compoundname>example</compoundname>
    <title>Examples</title>
    <detaileddescription>
<para>You can refer to the below stated example applications for more details which gives an idea about display port.</para><para>The 2 DisplayPort RX examples are as follows.</para><sect1 id="example_1ex1">
<title>xdp_rx_sst_dp159_example.c</title>
<para>This interrupt with timer example shows how to set up both the interrupt system with interrupt handlers and how to override the default sleep/delay functionality for MicroBlaze. A timer needs to exist in the hardware system and will be used for sleep/delay functionality inside of a callback function. The default behavior in MicroBlaze for sleep/delay is to call MB_Sleep from microblaze_sleep.h, which has only millisecond accuracy. For ARM/Zynq SoC systems, the supplied callback function will be ignored - the usleep function will be called since the SoC has a timer built-in. The programming sequence for the DP159 retimer is provided. This example uses the EmacPs&apos;s interrupt driven DMA packet transfer mode to send and receive frames.</para><para>For details, see <ref refid="xdp__rx__sst__dp159__example_8c" kindref="compound">xdp_rx_sst_dp159_example.c</ref>.</para></sect1>
<sect1 id="example_1ex2">
<title>xdp_rx_mst_example.c</title>
<para>This multi-stream transport (MST) example uses interrupts to detect events coming from the TX. The example sets up a topology that the RX will respond with, including LINK_ADDRESS reply and I2C and DPCD address spaces for the downstream ports.</para><para>For details, see <ref refid="xdp__rx__mst__example_8c" kindref="compound">xdp_rx_mst_example.c</ref>.</para><para><simplesect kind="note"><para>All example functions start with Dprx_*, while all driver functions start with XDp_*.</para></simplesect>
The 5 DisplayPort TX examples are as follows.</para></sect1>
<sect1 id="example_1ex3">
<title>xdp_tx_audio_example.c</title>
<para>This audio example, apart from training the main link and outputting video, illustrates the sequence required for setting up audio in the DisplayPort TX. This example requires that an audio source, such as a S/PDIF instance be present and connected to the DisplayPort TX in the hardware system, and for the audio enable configuration parameter to be set for the instantiated DisplayPort TX. For audio to output, the user will need to implement the following functions: a) Dptx_ConfigureAudioSrc : This function needs to configure the audio source to output to the DisplayPort TX. b) Dptx_AudioSendInfoFrame : This function needs to set up and write an audio info frame as per user requirements.</para><para>For details, see <ref refid="xdp__tx__audio__example_8c" kindref="compound">xdp_tx_audio_example.c</ref>.</para></sect1>
<sect1 id="example_1ex4">
<title>xdp_tx_intr_example.c</title>
<para>This interrupt example shows how to set up the interrupt system and specify the interrupt handlers for when a DisplayPort interrupt event occurs. An interrupt controller with a connection to the DisplayPort interrupt signal needs to exist in the hardware system.</para><para>For details, see <ref refid="xdp__tx__intr__example_8c" kindref="compound">xdp_tx_intr_example.c</ref>.</para></sect1>
<sect1 id="example_1ex5">
<title>xdp_tx_mst_example.c</title>
<para>This multi-stream transport (MST) example shows how to use the driver&apos;s MST capabilities. Streams can be directed at sinks using two methods: a) After topology discover has created a sink list, streams may be assigned to sinks using the index in the sink list. b) The streams may be assigned to sinks directly without using topology discovery if their relative addresses (and total number of DisplayPort links) from the DisplayPort source is known beforehand.</para><para>For details, see <ref refid="xdp__tx__mst__example_8c" kindref="compound">xdp_tx_mst_example.c</ref>.</para></sect1>
<sect1 id="example_1ex6">
<title>xdp_tx_poll_example.c</title>
<para>This interrupt example shows how to poll the DisplayPort TX instance&apos;s registers for DisplayPort interrupt events.</para><para>For details, see <ref refid="xdp__tx__poll__example_8c" kindref="compound">xdp_tx_poll_example.c</ref>.</para></sect1>
<sect1 id="example_1ex7">
<title>xdp_tx_timer_example.c</title>
<para>This timer example shows how to override the default sleep/delay functionality for MicroBlaze. A timer needs to exist in the hardware system and will be used for sleep/delay functionality inside of a callback function. The default behavior in MicroBlaze for sleep/delay is tocall MB_Sleep from microblaze_sleep.h, which has only millisecond accuracy. For ARM/Zynq SoC systems, the supplied callback function will be ignored - the usleep function will be called since the SoC has a timer built-in.</para><para>For details, see <ref refid="xdp__tx__timer__example_8c" kindref="compound">xdp_tx_timer_example.c</ref>.</para></sect1>
<sect1 id="example_1ex8">
<title>xdp_selftest_example.c</title>
<para>This self test example will perform a sanity check on the state of the DisplayPort TX/RX instance. It may be called prior to usage of the core or after a reset to ensure that (a subset of) the registers hold their default values.</para><para>For details, see <ref refid="xdp__selftest__example_8c" kindref="compound">xdp_selftest_example.c</ref>.</para></sect1>
<sect1 id="example_1ex9">
<title>xdp_tx_example_common.c</title>
<para>This example performs a self test on the DisplayPort TX core by training the main link at the maximum common capabilities between the TX and RX and checking the lane status.</para><para>For details, see <ref refid="xdp__tx__example__common_8c" kindref="compound">xdp_tx_example_common.c</ref>.</para></sect1>
<sect1 id="example_1ex10">
<title>xdp_tx_example_common.h</title>
<para>This is the header file which is common for all examples.</para><para>For details, see <ref refid="xdp__tx__example__common_8h" kindref="compound">xdp_tx_example_common.h</ref>.</para><sect2 id="example_1IMPORTANT">
<title>NOTES</title>
<para><itemizedlist>
<listitem><para>Each of these examples are meant to be used in conjunction with xdp_tx_example_common.[ch] which holds common functionality for all examples. After importing the examples, these files will need to be manually copied into the example src/ directory. This code shows how to train the main link and set up a video stream.</para></listitem><listitem><para>Additionally, in order to be able to use the interrupt, polling, and timer examples, the user will need to implement and link the following functions: 1) Dptx_InitPlatform : This function needs to do all hardware system initialization. 2) Dptx_StreamSrc* : These function need to configure the source of the stream (pattern generators, video input, etc.) such that a video stream, with timings and video attributes that correspond to the main stream attributes (MSA) configuration, is received by the DisplayPort Tx. The examples call this function from the Dptx_Run-&gt;Dptx_StartVideoStream functions in <ref refid="xdp__tx__example__common_8c" kindref="compound">xdp_tx_example_common.c</ref>.</para></listitem></itemizedlist>
</para><para><simplesect kind="note"><para>All example functions start with Dptx_*, while all driver functions start with XDp_*. </para></simplesect>
</para></sect2>
</sect1>
    </detaileddescription>
  </compounddef>
</doxygen>
