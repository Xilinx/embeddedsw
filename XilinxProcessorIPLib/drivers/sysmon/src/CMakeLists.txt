# Copyright (C) 2023 - 2026 Advanced Micro Devices, Inc.  All rights reserved.
# Copyright (c) 2021 Xilinx, Inc.  All rights reserved.
# SPDX-License-Identifier: MIT
cmake_minimum_required(VERSION 3.15)
project(sysmon)
set(SYSMON_DIR "${CMAKE_CURRENT_SOURCE_DIR}")
find_package(SYSMON QUIET)
set(_SYSMON_IS_SYSTEM_MGMT FALSE)
if ((EXISTS "${SYSMON_DIR}/SYSMONConfig.cmake") AND DRIVER_INSTANCES)
   foreach(ELEMENT ${TOTAL_DRIVER_PROP_LIST})
      list(GET ${ELEMENT} 3 _ip_type)
      if("${_ip_type}" STREQUAL "0x1")
         set(_SYSMON_IS_SYSTEM_MGMT TRUE)
      endif()
   endforeach()
endif()
if(_SYSMON_IS_SYSTEM_MGMT OR "${CMAKE_MACHINE}" STREQUAL "ZynqMP")
   add_definitions(-DXSM_ZYNQMP)
else()
   add_definitions(-DXSM_ZYNQ)
endif()
find_package(common)
collector_create (PROJECT_LIB_HEADERS "${CMAKE_CURRENT_SOURCE_DIR}")
collector_create (PROJECT_LIB_SOURCES "${CMAKE_CURRENT_SOURCE_DIR}")
include_directories(${CMAKE_BINARY_DIR}/include)
collect (PROJECT_LIB_SOURCES xsysmon.c)
collect (PROJECT_LIB_HEADERS xsysmon.h)
collect (PROJECT_LIB_SOURCES xsysmon_g.c)
collect (PROJECT_LIB_HEADERS xsysmon_hw.h)
collect (PROJECT_LIB_SOURCES xsysmon_intr.c)
collect (PROJECT_LIB_SOURCES xsysmon_selftest.c)
collect (PROJECT_LIB_SOURCES xsysmon_sinit.c)
collector_list (_sources PROJECT_LIB_SOURCES)
collector_list (_headers PROJECT_LIB_HEADERS)
file(COPY ${_headers} DESTINATION ${CMAKE_BINARY_DIR}/include)
add_library(sysmon STATIC ${_sources})
set_target_properties(sysmon PROPERTIES LINKER_LANGUAGE C)
