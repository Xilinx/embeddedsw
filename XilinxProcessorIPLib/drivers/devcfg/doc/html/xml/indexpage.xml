<?xml version='1.0' encoding='UTF-8' standalone='no'?>
<doxygen xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="compound.xsd" version="1.8.5">
  <compounddef id="indexpage" kind="page">
    <compoundname>index</compoundname>
    <title>devcfg</title>
    <detaileddescription>
<para>The is the main header file for the Device Configuration Interface of the Zynq device. The device configuration interface has three main functionality.<orderedlist>
<listitem><para>AXI-PCAP</para></listitem><listitem><para>Security Policy</para></listitem><listitem><para>XADC This current version of the driver supports only the AXI-PCAP and Security Policy blocks. There is a separate driver for XADC.</para></listitem></orderedlist>
</para><para>AXI-PCAP is used for download/upload an encrypted or decrypted bitstream. DMA embedded in the AXI PCAP provides the master interface to the Device configuration block for any DMA transfers. The data transfer can take place between the Tx/RxFIFOs of AXI-PCAP and memory (on chip RAM/DDR/peripheral memory).</para><para>The current driver only supports the downloading the FPGA bitstream and readback of the decrypted image (sort of loopback). The driver does not know what information needs to be written to the FPGA to readback FPGA configuration register or memory data. The application above the driver should take care of creating the data that needs to be downloaded to the FPGA so that the bitstream can be readback. This driver also does not support the reading of the internal registers of the PCAP. The driver has no knowledge of the PCAP internals.</para><para><bold> Initialization and Configuration </bold></para><para>The device driver enables higher layer software (e.g., an application) to communicate with the Device Configuration device.</para><para><ref refid="group__devcfg_1gafcaaa8ac67cf7316c54d1cba36e83e08" kindref="member">XDcfg_CfgInitialize()</ref> API is used to initialize the Device Configuration Interface. The user needs to first call the <ref refid="group__devcfg_1ga6f1dabd32e61821d2c089f77aa80f7f4" kindref="member">XDcfg_LookupConfig()</ref> API which returns the Configuration structure pointer which is passed as a parameter to the <ref refid="group__devcfg_1gafcaaa8ac67cf7316c54d1cba36e83e08" kindref="member">XDcfg_CfgInitialize()</ref> API.</para><para><bold>Interrupts</bold> The Driver implements an interrupt handler to support the interrupts provided by this interface.</para><para><bold> Threads </bold></para><para>This driver is not thread safe. Any needs for threads or thread mutual exclusion must be satisfied by the layer above this driver.</para><para><bold> Asserts </bold></para><para>Asserts are used within all Xilinx drivers to enforce constraints on argument values. Asserts can be turned off on a system-wide basis by defining, at compile time, the NDEBUG identifier. By default, asserts are turned on and it is recommended that users leave asserts on during development.</para><para><bold> Building the driver </bold></para><para>The <ref refid="struct_x_dcfg" kindref="compound">XDcfg</ref> driver is composed of several source files. This allows the user to build and link only those parts of the driver that are necessary.</para><para><linebreak/>
<linebreak/>
</para><para><preformatted>
MODIFICATION HISTORY:</preformatted></para><para><preformatted>Ver   Who Date     Changes
<hruler/>

1.00a hvm 02/07/11 First release
2.00a nm  05/31/12 Updated the driver for CR 660835 so that input length for
             source/destination to the XDcfg_InitiateDma, XDcfg_Transfer
             APIs is words (32 bit) and not bytes.
                     Updated the notes for XDcfg_InitiateDma/XDcfg_Transfer APIs
             to add information that 2 LSBs of the Source/Destination
             address when equal to 2�b01 indicate the last DMA command
             of an overall transfer.
             Destination Address passed to this API for secure transfers
             instead of using 0xFFFFFFFF for CR 662197. This issue was
             resulting in the failure of secure transfers of
             non-bitstream images.
2.01a nm  07/07/12 Updated the XDcfg_IntrClear function to directly
             set the mask instead of oring it with the
             value read from the interrupt status register
                     Added defines for the PS Version bits,
                   removed the FIFO Flush bits from the
             Miscellaneous Control Reg.
             Added XDcfg_GetPsVersion, XDcfg_SelectIcapInterface
             and XDcfg_SelectPcapInterface APIs for CR 643295
             The user has to call the XDcfg_SelectIcapInterface API
             for the PL reconfiguration using AXI HwIcap.
             Updated the XDcfg_Transfer API to clear the
             QUARTER_PCAP_RATE_EN bit in the control register for
             non secure writes for CR 675543.
2.02a nm  01/31/13 Fixed CR# 679335.
                     Added Setting and Clearing the internal PCAP loopback.
             Removed code for enabling/disabling AES engine as BootROM
             locks down this setting.
             Fixed CR# 681976.
             Skip Checking the PCFG_INIT in case of non-secure DMA
             loopback.
             Fixed CR# 699558.
             XDcfg_Transfer fails to transfer data in loopback mode.
             Fixed CR# 701348.
                   Peripheral test fails with  Running
                     <ref refid="xdevcfg__selftest__example_8c_1afcc9080c658869d88656386b9337961b" kindref="member">DcfgSelfTestExample()</ref> in SECURE bootmode.
2.03a nm  04/19/13 Fixed CR# 703728.
             Updated the register definitions as per the latest TRM
             version UG585 (v1.4) November 16, 2012.
3.0   adk 10/12/13 Updated as per the New Tcl API&apos;s
3.0   kpc 21/02/14 Added function prototype for XDcfg_ClearControlRegister
3.2   sb  08/25/14 Fixed XDcfg_PcapReadback() function
             updated driver code with != instead of ==,
             while checking for Interrupt Status with DMA and
             PCAP Done Mask
             ((XDcfg_ReadReg(InstancePtr-&gt;Config.BaseAddr,
                XDCFG_INT_STS_OFFSET) &amp;
                XDCFG_IXR_D_P_DONE_MASK) !=
                XDCFG_IXR_D_P_DONE_MASK);
             A new example has been added to read back the
             configuration registers from the PL region.
             <ref refid="xdevcfg__reg__readback__example_8c" kindref="compound">xdevcfg_reg_readback_example.c</ref>
3.3   sk  04/06/15 Modified XDcfg_ReadMultiBootConfig Macro CR# 851335.
      ms  03/17/17 Added readme.txt file in examples folder for doxygen
                   generation.
      ms  04/10/17 Modified filename tag in interrupt and polled examples
                   to include them in doxygen examples.
3.5   ms  04/18/17 Modified tcl file to add suffix U for all macros
                   definitions of devcfg in xparameters.h
      ms  08/07/17 Fixed compilation warnings in <ref refid="xdevcfg__sinit_8c" kindref="compound">xdevcfg_sinit.c</ref>
3.8  Nava 06/21/23 Added support for system device-tree flow.
</preformatted> </para>    </detaileddescription>
  </compounddef>
</doxygen>
