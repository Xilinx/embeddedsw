<?xml version='1.0' encoding='UTF-8' standalone='no'?>
<doxygen xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="compound.xsd" version="1.8.5">
  <compounddef id="example" kind="page">
    <compoundname>example</compoundname>
    <title>Examples</title>
    <detaileddescription>
<para>You can refer to the below stated example applications for more details on how to use rfdc driver.</para><sect1 id="example_1ex1">
<title>xrfdc_selftest_example.c</title>
<para>contains a selftest example for using the rfdc hardware and RFSoC Data Converter driver. This example does some writes to the hardware to do some sanity checks and does a reset to restore the original settings.</para><para>For details, see <ref refid="xrfdc__selftest__example_8c" kindref="compound">xrfdc_selftest_example.c</ref>.</para></sect1>
<sect1 id="example_1ex2">
<title>xrfdc_read_write_example.c</title>
<para>Contains an example to use multiple driver &quot;set&quot; APIs to configure the targeted AMS block. Subsequently it uses &quot;get&quot; APIs to read back the configurations to ensure that the desired configurations are applied. For DAC it sets the following configurations: MixerSettings, QMCSettings, Write Fabricrate, Decoder mode, Output Current and Coarse Delay. For ADC it sets the following configurations: MixerSettings, QMCSettings, Read Fabricrate and Threshold Settings. This example shows how to change the configurations for ADC and DAC using driver functions. NOTE: The purpose of the example is to show how to use the driver APIs. For real user scenarios this example will not be relevant.</para><para>For details, see <ref refid="xrfdc__read__write__example_8c" kindref="compound">xrfdc_read_write_example.c</ref>.</para></sect1>
<sect1 id="example_1ex3">
<title>xrfdc_intr_example.c</title>
<para>Contains an example to show the interrupts, interrupts are mostly used for error reporting. The interrupts do not do any data processing. Since they dont do any data processing, interrupts are invoked in rare conditions. The example here attempts to demonstrate users how an error interrupt can be generated. Also once generated how does the processing happen. Upon an interrupt, the control reaches to ScuGIC interrupt handler. From there the control is transferred to the libmetal isr handling which then calls the driver interrupt handler. Users are expected to register their callbacks with the driver interrupt framework. The actual interrupt handling is expected to happen in the user provided callback. This example generates ADC fabric interrupts by writing some incorrect fabric data rate based on the read/write clocks.</para><para>For details, see <ref refid="xrfdc__intr__example_8c" kindref="compound">xrfdc_intr_example.c</ref>. </para></sect1>
    </detaileddescription>
  </compounddef>
</doxygen>
