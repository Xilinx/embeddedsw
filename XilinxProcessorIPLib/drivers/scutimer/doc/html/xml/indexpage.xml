<?xml version='1.0' encoding='UTF-8' standalone='no'?>
<doxygen xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="compound.xsd" version="1.8.5">
  <compounddef id="indexpage" kind="page">
    <compoundname>index</compoundname>
    <title>scutimer</title>
    <detaileddescription>
<para>The timer driver supports the Cortex A9 private timer.The timer driver supports the following features:<itemizedlist>
<listitem><para>Normal mode and Auto reload mode</para></listitem><listitem><para>Interrupts (Interrupt handler is not provided in this driver. Application has to register it&apos;s own handler)</para></listitem></itemizedlist>
</para><para><bold> Initialization and Configuration </bold></para><para>The device driver enables higher layer software (e.g., an application) to communicate with the Timer.</para><para><ref refid="group__scutimer_1ga59badd651be237aab4099cedd96621d2" kindref="member">XScuTimer_CfgInitialize()</ref> API is used to initialize the Timer. The user needs to first call the <ref refid="group__scutimer_1ga58903096d235b860a31f50c4ab70c853" kindref="member">XScuTimer_LookupConfig()</ref> API which returns the Configuration structure pointer which is passed as a parameter to the <ref refid="group__scutimer_1ga59badd651be237aab4099cedd96621d2" kindref="member">XScuTimer_CfgInitialize()</ref> API.</para><para><bold> Interrupts </bold></para><para>The Timer hardware supports interrupts.</para><para>This driver does not provide a Interrupt Service Routine (ISR) for the device. It is the responsibility of the application to provide one if needed. Refer to the interrupt example provided with this driver for details on using the Timer in interrupt mode.</para><para><bold> Virtual Memory </bold></para><para>This driver supports Virtual Memory. The RTOS is responsible for calculating the correct device base address in Virtual Memory space.</para><para><bold> Threads </bold></para><para>This driver is not thread safe. Any needs for threads or thread mutual exclusion must be satisfied by the layer above this driver.</para><para><bold> Asserts </bold></para><para>Asserts are used within all Xilinx drivers to enforce constraints on argument values. Asserts can be turned off on a system-wide basis by defining, at compile time, the NDEBUG identifier. By default, asserts are turned on and it is recommended that users leave asserts on during development.</para><para><bold> Building the driver </bold></para><para>The <ref refid="struct_x_scu_timer" kindref="compound">XScuTimer</ref> driver is composed of several source files. This allows the user to build and link only those parts of the driver that are necessary.</para><para><linebreak/>
<linebreak/>
</para><para>NOTE: The timer is not a part of the snoop control unit as indicated by the prefix &quot;scu&quot; in the name of the driver. It is an independent module in APU.</para><para><preformatted>
MODIFICATION HISTORY:</preformatted></para><para><preformatted>Ver   Who Date     Changes
<hruler/>

1.00a nm  03/10/10 First release
1.02a sg  07/17/12 Included xil_assert.h for CR 667947. This is an issue
             when the xstatus.h in the common driver overwrites
             the xstatus.h of the standalone BSP during the
             libgen.
2.1     sk  02/26/15 Modified the code for MISRA-C:2012 compliance.
      ms  03/17/17 Added readme.txt file in examples folder for doxygen
                   generation.
2.3   mus 08/31/20 Updated makefile to support parallel make and
                   incremental builds, it would help to reduce compilation
                   time.
2.5   dp   07/11/23 Add support for system device tree flow
</preformatted> </para>    </detaileddescription>
  </compounddef>
</doxygen>
