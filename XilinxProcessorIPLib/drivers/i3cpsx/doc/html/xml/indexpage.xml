<?xml version='1.0' encoding='UTF-8' standalone='no'?>
<doxygen xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="compound.xsd" version="1.8.5">
  <compounddef id="indexpage" kind="page">
    <compoundname>index</compoundname>
    <title>i3cpsx</title>
    <detaileddescription>
<para>This is an implementation of I3C driver in the PS block. The device can be either a master or a slave on the I3C bus. This implementation supports both interrupt mode transfer and polled mode transfer.</para><para>The higher level software must implement a higher layer protocol to inform the slave what to send to the master.</para><para><bold>Initialization &amp; Configuration</bold></para><para>The <ref refid="struct_x_i3c_psx___config" kindref="compound">XI3cPsx_Config</ref> structure is used by the driver to configure itself. This configuration structure is typically created by the tool-chain based on HW build properties.</para><para>To support multiple runtime loading and initialization strategies employed by various operating systems, the driver instance can be initialized in the following way:</para><para><itemizedlist>
<listitem><para>XI3cPsx_LookupConfig(DeviceId) - Use the device identifier to find the static configuration structure defined in XI3cPsx_g.c. This is setup by the tools. For some operating systems the config structure will be initialized by the software and this call is not needed.</para></listitem><listitem><para>XI3cPsx_CfgInitialize(InstancePtr, CfgPtr, EffectiveAddr) - Uses a configuration structure provided by the caller. If running in a system with address translation, the provided virtual memory base address replaces the physical address in the configuration structure.</para></listitem></itemizedlist>
</para><para><bold>Multiple Masters</bold></para><para><bold>Multiple Slaves</bold></para><para>Multiple slaves are supported by selecting them with unique addresses.</para><para><bold>Polled Mode Operation</bold></para><para>This driver supports polled mode transfers.</para><para><bold>Interrupts</bold></para><para>The user must connect the interrupt handler of the driver, XI3cPsx_InterruptHandler to an interrupt system such that it will be called when an interrupt occurs. This function does not save and restore the processor context such that the user must provide this processing.</para><para>The driver handles the following interrupts:<itemizedlist>
<listitem><para>Transfer complete</para></listitem><listitem><para>More Data</para></listitem><listitem><para>Error</para></listitem></itemizedlist>
</para><para><preformatted> MODIFICATION HISTORY:</preformatted></para><para><preformatted>Ver   Who     Date     Changes
<hruler/>

1.00  sd  06/10/22 First release
1.3  sd   11/17/23 Added support for system device-tree flow
1.4  gm   10/07/24 Added functions for Enable, Resume, read response
                     and set threshold for Tx, Rx and command.
                     Update data type of Send and Recv byte counts.
1.7  vlt  12/30/25 Update Doxygen comments to include SDT flow details.
     vlt  01/27/26 Fixed codespell issues.
     vlt  03/15/26 Updated BaseAddress type from u32 to UINTPTR
                   to support 64-bit addressing.
</preformatted> </para>    </detaileddescription>
  </compounddef>
</doxygen>
