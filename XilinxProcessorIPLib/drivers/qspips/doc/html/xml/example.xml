<?xml version='1.0' encoding='UTF-8' standalone='no'?>
<doxygen xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="compound.xsd" version="1.8.5">
  <compounddef id="example" kind="page">
    <compoundname>example</compoundname>
    <title>Examples</title>
    <detaileddescription>
<para>You can refer to the below stated example applications for more details on how to use qspips driver.</para><sect1 id="example_1ex1">
<title>xqspips_selftest_example.c</title>
<para>Contains an example on how to use the XQspips driver directly. This example performs the basic selftest using the driver.</para><para>For details, see <ref refid="xqspips__selftest__example_8c" kindref="compound">xqspips_selftest_example.c</ref>.</para></sect1>
<sect1 id="example_1ex2">
<title>xqspips_dual_flash_lqspi_example.c</title>
<para>Contains an example on how to use the XQspips driver directly. This example shows the usage of the QSPI driver in Linear QSPI mode, with two serial FLASH devices on separate buses. With two flash memories on separate buses, even numbered bits in data words are written to the lower memory and odd numbered bits are written to the upper memory. This example writes to the two flash memories in QSPI mode and reads the data back from the flash memories, in Linear QSPI mode.</para><para>For details, see <ref refid="xqspips__dual__flash__lqspi__example_8c" kindref="compound">xqspips_dual_flash_lqspi_example.c</ref>.</para></sect1>
<sect1 id="example_1ex3">
<title>xqspips_dual_flash_stack_lqspi_example.c</title>
<para>Contains an example on how to use the XQspips driver directly. This example shows the usage of the QSPI driver in Linear QSPI mode, with two serial Flash devices in stacked mode. One flash is accessed at a time on a common bus by using separate selects. This example writes to the two flash memories in QSPI mode and reads the data back from the flash memories, in Linear QSPI mode.</para><para>For details, see <ref refid="xqspips__dual__flash__stack__lqspi__example_8c" kindref="compound">xqspips_dual_flash_stack_lqspi_example.c</ref>.</para></sect1>
<sect1 id="example_1ex4">
<title>xqspips_flash_intr_example.c</title>
<para>Contains an example on how to use the XQspips driver directly. This example shows the usage of the QSPI driver in interrupt mode with a serial FLASH device. This examples performs some transfers in Manual Chip Select and Start mode.</para><para>For details, see <ref refid="xqspips__flash__intr__example_8c" kindref="compound">xqspips_flash_intr_example.c</ref>.</para></sect1>
<sect1 id="example_1ex5">
<title>xqspips_flash_lqspi_example.c</title>
<para>Contains an example on how to use the XQspips driver directly. This example shows the usage of the QSPI driver in Linear QSPI mode with a serial FLASH device. The example writes to the flash in QSPI mode and reads it back in Linear QSPI mode.</para><para>For details, see <ref refid="xqspips__flash__lqspi__example_8c" kindref="compound">xqspips_flash_lqspi_example.c</ref>.</para></sect1>
<sect1 id="example_1ex6">
<title>xqspips_flash_polled_example.c</title>
<para>Contains an example on how to use the XQspips driver directly. This example shows the usage of the QSPI driver in polled mode with a serial FLASH device.</para><para>For details, see <ref refid="xqspips__flash__polled__example_8c" kindref="compound">xqspips_flash_polled_example.c</ref>.</para></sect1>
<sect1 id="example_1ex7">
<title>xqspips_g128_flash_example.c</title>
<para>Contains an example on how to use the XQspips driver directly. This example shows the usage of the QSPI driver with a serial Flash device greater than 128Mb. The example writes to flash and reads it back in I/O mode.</para><para>For details, see <ref refid="xqspips__g128__flash__example_8c" kindref="compound">xqspips_g128_flash_example.c</ref>. </para></sect1>
    </detaileddescription>
  </compounddef>
</doxygen>
