<?xml version='1.0' encoding='UTF-8' standalone='no'?>
<doxygen xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="compound.xsd" version="1.8.5">
  <compounddef id="example" kind="page">
    <compoundname>example</compoundname>
    <title>Examples</title>
    <detaileddescription>
<para>You can refer to the below stated example applications for more details on how to use spi driver.</para><sect1 id="example_1ex1">
<title>xspi_selftest_example.c</title>
<para>Contains an example on how to use the <ref refid="struct_x_spi" kindref="compound">XSpi</ref> driver directly. This example performs the basic selftest using the driver.</para><para>For details, see <ref refid="xspi__selftest__example_8c" kindref="compound">xspi_selftest_example.c</ref>.</para></sect1>
<sect1 id="example_1ex2">
<title>xspi_atmel_flash_example.c</title>
<para>Contains an example on how to use the <ref refid="struct_x_spi" kindref="compound">XSpi</ref> driver directly. This example shows the usage of the SPI driver and hardware device with an Atmel Serial Flash Device (AT45XX series). This example erases the Page, writes to the Page, reads back from the Page and compares the data.</para><para>For details, see <ref refid="xspi__atmel__flash__example_8c" kindref="compound">xspi_atmel_flash_example.c</ref>.</para></sect1>
<sect1 id="example_1ex3">
<title>xspi_eeprom_example.c</title>
<para>Contains an example on how to use the <ref refid="struct_x_spi" kindref="compound">XSpi</ref> driver directly. This example shows the usage of the SPI driver and hardware device with a serial EEPROM device.</para><para>For details, see <ref refid="xspi__eeprom__example_8c" kindref="compound">xspi_eeprom_example.c</ref>.</para></sect1>
<sect1 id="example_1ex4">
<title>xspi_intel_flash_example.c</title>
<para>Contains an example on how to use the <ref refid="struct_x_spi" kindref="compound">XSpi</ref> driver directly. This example shows the usage of the SPI driver and hardware device with an Intel Serial Flash Memory(S33) in the interrupt mode. This example erases a sector, writes to a Page within the sector, reads back from that Page and compares the data.</para><para>For details, see <ref refid="xspi__intel__flash__example_8c" kindref="compound">xspi_intel_flash_example.c</ref>.</para></sect1>
<sect1 id="example_1ex5">
<title>xspi_intr_example.c</title>
<para>Contains an example on how to use the <ref refid="struct_x_spi" kindref="compound">XSpi</ref> driver directly. This example shows the usage of the Spi driver and the Spi device using the interrupt mode.</para><para>For details, see <ref refid="xspi__intr__example_8c" kindref="compound">xspi_intr_example.c</ref>.</para></sect1>
<sect1 id="example_1ex6">
<title>xspi_low_level_example.c</title>
<para>Contains an example on how to use the <ref refid="struct_x_spi" kindref="compound">XSpi</ref> driver directly. This example shows the usage of the low-level driver of the SPI driver. These macros are found in <ref refid="xspi__l_8h" kindref="compound">xspi_l.h</ref>. A simple loopback test is done within an SPI device in polled mode. This example works only with 8-bit wide data transfers.</para><para>For details, see <ref refid="xspi__low__level__example_8c" kindref="compound">xspi_low_level_example.c</ref>.</para></sect1>
<sect1 id="example_1ex7">
<title>xspi_numonyx_flash_quad_example.c</title>
<para>Contains an example on how to use the <ref refid="struct_x_spi" kindref="compound">XSpi</ref> driver directly. This example shows the usage of the SPI driver and axi_qspi device with a Numonyx quad serial flash device in the interrupt mode. This example erases a Sector, writes to a Page within the Sector, reads back from that Page and compares the data.</para><para>For details, see <ref refid="xspi__numonyx__flash__quad__example_8c" kindref="compound">xspi_numonyx_flash_quad_example.c</ref>.</para></sect1>
<sect1 id="example_1ex8">
<title>xspi_polled_example.c</title>
<para>Contains an example on how to use the <ref refid="struct_x_spi" kindref="compound">XSpi</ref> driver directly. This example shows the usage of the Spi driver and the Spi device using the polled mode.</para><para>For details, see <ref refid="xspi__polled__example_8c" kindref="compound">xspi_polled_example.c</ref>.</para></sect1>
<sect1 id="example_1ex9">
<title>xspi_slave_intr_example.c</title>
<para>Contains an example on how to use the <ref refid="struct_x_spi" kindref="compound">XSpi</ref> driver directly. This example shows the usage of the Spi driver and the Spi device as a Slave, in interrupt mode.</para><para>For details, see <ref refid="xspi__slave__intr__example_8c" kindref="compound">xspi_slave_intr_example.c</ref>.</para></sect1>
<sect1 id="example_1ex10">
<title>xspi_slave_polled_example.c</title>
<para>Contains an example on how to use the <ref refid="struct_x_spi" kindref="compound">XSpi</ref> driver directly. This example shows the usage of the Spi driver and the Spi device as a Slave, in polled mode.</para><para>For details, see <ref refid="xspi__slave__polled__example_8c" kindref="compound">xspi_slave_polled_example.c</ref>.</para></sect1>
<sect1 id="example_1ex11">
<title>xspi_stm_flash_example.c</title>
<para>Contains an example on how to use the <ref refid="struct_x_spi" kindref="compound">XSpi</ref> driver directly. This example shows the usage of the SPI driver and hardware device with an STM serial Flash device (M25P series) in the interrupt mode. This example erases a Sector, writes to a Page within the Sector, reads back from that Page and compares the data.</para><para>For details, see <ref refid="xspi__stm__flash__example_8c" kindref="compound">xspi_stm_flash_example.c</ref>.</para></sect1>
<sect1 id="example_1ex12">
<title>xspi_winbond_flash_quad_example.c</title>
<para>Contains an example on how to use the <ref refid="struct_x_spi" kindref="compound">XSpi</ref> driver directly. This example shows the usage of the SPI driver and axi_qspi device with a Winbond quad serial flash device in the interrupt mode. This example erases a Sector, writes to a Page within the Sector, reads back from that Page and compares the data.</para><para>For details, see <ref refid="xspi__winbond__flash__quad__example_8c" kindref="compound">xspi_winbond_flash_quad_example.c</ref>.</para></sect1>
<sect1 id="example_1ex13">
<title>xspi_winbond_flash_xip_example.c</title>
<para>Contains an example on how to use the <ref refid="struct_x_spi" kindref="compound">XSpi</ref> driver directly. This example shows the usage of the Spi driver and the Spi device configured in XIP Mode. This example reads data from the Flash Memory in the way RAM is accessed.</para><para>For details, see <ref refid="xspi__winbond__flash__xip__example_8c" kindref="compound">xspi_winbond_flash_xip_example.c</ref>. </para></sect1>
    </detaileddescription>
  </compounddef>
</doxygen>
